# env / secrets — never commit API keys
.env
.env.*

# OS / editor
.DS_Store
*.swp

# scratch renders / audio (skills write into a separate studio/ repo, not here)
*.mp4
*.mp3
*.wav
/tmp/

# HyperFrames per-project generated artifacts (regenerable — commit source only)
.hyperframes/
.thumbnails/
snapshots/
renders/

# python
__pycache__/
*.pyc
